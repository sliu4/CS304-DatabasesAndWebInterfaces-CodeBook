{% extends "home.html"%}
{% block body %}
<p>Please submit a function to add to CodeBook. Requires admin approval for function to have been fully added.</p>
<div>
  <form method=POST action="/add/">
    <table>
      <tr>
        <td><label for="function-language" accesskey="function-language">Function Language</label></td>
        <td>
	  <select name="function-language">
	    <option value="none">Choose One</option>
	    {% for language in languages %}
	    <option id="function-language" value="{{language.abbreviation}}">{{language.name}}</option>
	    {% endfor %}
	  </select>
	</td>
      </tr>
      <tr>
        <td><label for="function-name" accesskey="function-name">Function Name</label></td>
        <td><input name="function-name" id="function-name"></td>
      </tr>
      <tr>
        <td><label for="function-description" accesskey="function-description">Description</label></td>
        <td><textarea rows="4" cols="23"name="function-description" id="function-description"></textarea></td>
      </tr>
      <tr>
        <td><label for="function-url" accesskey="function-url">Documentation URL</label></td>
        <td><input name="function-url" id="function-url"></td>
      </tr>
    </table>
    <input type="submit" name="submit" value="add">
  </form>
</div>
{% endblock %}
